using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RockException : MonoBehaviour
{
    // Start is called before the first frame update
    public bool rockDisponible;
    void Start()
    {
        rockDisponible = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
